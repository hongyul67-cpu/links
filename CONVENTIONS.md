# 학습도구 제작 규약

새 도구를 만들거나 기존 도구를 고칠 때 지키는 규칙입니다.
**작업 시작 전에 이 문서를 먼저 읽으세요.**

---

## 0. 저장소 구조

| 구분 | 위치 | 브랜치 |
|---|---|---|
| 공용 스크립트 | `hongyul67-cpu/links` | **master** |
| 각 학습도구 | 도구마다 별도 저장소 (예: `domyeon-master`, `paper-scale-master`) | **main** |

`links/` 안의 파일은 **모든 도구가 함께 씁니다.** 하나를 고치면 20개가 넘는 도구가 동시에 영향을 받습니다.
도구 하나를 고치려다 공용 파일을 바꾸게 되면, 먼저 담당자에게 알리세요.

| 파일 | 역할 |
|---|---|
| `collector.js` | 학생 결과를 교사 구글시트로 제출 |
| `Code.gs` | 시트 쪽 백엔드 (Apps Script 웹 앱) |
| `fx.js` | 정답 파티클·흔들림·클리어 배너 등 연출 |
| `rank.js` | 아이언~마스터 계급과 랭크 포인트 |
| `class-mode.js` | 수업용 큰 화면 모드 |
| `reset.js` | 이 기기에 쌓인 학습 기록 지우기 (공용 PC용) |
| `link-builder.html` | 교사용 제출 링크 생성기 |
| `guide.html` | 교사용 결과수집 설정 안내 |

---

## 1. 결과 제출

### 붙이는 법

```html
<!-- </body> 앞 -->
<script src="https://hongyul67-cpu.github.io/links/collector.js"
        data-tool="도구 이름"></script>
```

`endpoint`는 코드에 적지 않습니다. 교사별 링크의 `?rc=...`로 주입됩니다.

### 부르는 법 — `attach()` 를 쓰세요

채점이 끝나는 자리에서 **한 줄**이면 됩니다.

```js
ResultCollector.attach(
  기준요소,                                  // 이 요소 바로 뒤에 버튼이 붙음
  () => ({ score, correct, total, wrong, durationSec }),
  { mode: '도구명 — 파트명', extra: ['생기부 키워드'] }
);
```

`attach()`가 알아서 해 주는 것:
- 제출용 링크(`?rc=`)가 없어도 **버튼을 보여 주고**, 누르면 왜 안 되는지 안내
- 같은 파트를 몇 번째 푸는지(`retry`) 세어 함께 전송
- `Rank`가 있으면 계급을 함께 전송

> 옛 방식인 `ResultCollector.open()` 직접 호출은 새 코드에 쓰지 마세요.
> 화면 구조가 특이해 `attach()`를 못 쓰는 경우에만 `open()`을 쓰되,
> `mode` · `retry` · `tier`를 직접 채워 넣어야 합니다.

### 반드시 지킬 것

**① `mode`는 파트마다 다르게.** 시트의 `활동(파트)` 열에 그대로 들어갑니다.

```js
mode: '선의 종류 — 오류 찾기'      // ✔ 어느 파트인지 남음
mode: '도면읽기'                   // ✘ 전체 결과만 남아 쓸모없음
```

**② `wrong`은 번호만 넣지 말고 무엇을 틀렸는지 적을 것.**

```js
wrong: ['3번 절단선→중심선', '7번 숨은선(은선)→외형선']   // ✔
wrong: [3, 7]                                            // ✘ 뭘 틀렸는지 모름
```

**③ 제출 버튼은 항상 보이게.** 제출용 링크가 아니라고 숨기면, 학생은 버튼이 있는 줄도 모릅니다.

### 시트에 쌓이는 열

```
제출시각 · 반 · 번호 · 이름 · 학년 · 학과 · 활동(파트) ·
점수 · 정답수 · 총문항 · 정답률(%) · 틀린 문제 · 소요(초) ·
도전 횟수 · 계급 · 기기 · 활동 키워드(생기부용) · 세특 문장(초안)
```

`활동 키워드`와 `세특 문장`은 학생이 쓰는 게 아니라, 파트·정답률·소요시간·재도전
횟수로 `collector.js`가 자동 생성합니다.

> **주의 — 열 이름은 탭이 처음 만들어질 때만 붙습니다.**
> `Code.gs`의 열을 바꾸면 기존 탭에는 반영되지 않습니다.
> 열을 추가·변경했다면 **반드시 담당 교사에게 "Code.gs를 다시 붙여넣고
> 새 버전으로 배포해야 하고, 기존 탭은 지우거나 시트를 새로 만들어야 한다"고 알리세요.**

---

## 2. 문항 구성

| | 연습 모드 | 종합시험 |
|---|---|---|
| 문항 수 | 8문항 | **20문항** |
| 제출 직후 | 정답·해설을 **바로** 보여줌 | **아무것도 알려주지 않음** |
| 진행 중 점수 | 실시간 표시 | 감춤 ("채점은 마지막에") |
| 정답 연출 | 파티클·흔들림 | 없음 |
| 끝난 뒤 | 점수 + 배너 | **오답노트** → 그 아래 제출 버튼 |

**오답노트에 넣을 것**: 문제 번호 · 문제 그림 · 지문 · 내가 고른 답 · 정답 · 해설

시험 중에 정답이 보이면 시험이 아니라 연습이 됩니다. 정답 표시, 해설, 효과음,
점수판까지 **전부** 감춰야 합니다. 그림에 정답이 그려지는 도구(소실점 표시 등)는
그림도 문제 상태로 되돌려야 합니다.

---

## 3. 연출 · 계급 · 수업모드

```html
<script src="https://hongyul67-cpu.github.io/links/fx.js"></script>
<script src="https://hongyul67-cpu.github.io/links/class-mode.js"></script>
<script src="https://hongyul67-cpu.github.io/links/rank.js" data-tool="제도 마스터"></script>
```

- **fx.js** — `FX.burst / punch / shake / flash / banner / starsFor`
  도구가 자체 채점 화면을 가지고 있으면 `FX.ok` / `FX.no` 대신 개별 효과를 쓰세요.
- **rank.js** — `Rank.card()` `Rank.badge()` `Rank.award(점수0~100, {mode})` `Rank.resultBox()`
  `data-tool`이 **같으면 점수가 합산**됩니다. 같은 계열 도구는 같은 이름을 쓰세요.
- **class-mode.js** — 한 줄만 붙이면 됩니다. `.wrap / .tab / .choice / .explain` 구조를 자동 인식합니다.

---

## 3-1. 기록 초기화

교실 공용 PC에서는 앞 학생 기록이 남아 다음 학생에게 그대로 보입니다.
모든 학습도구에 아래 한 줄을 붙여 주세요.

```html
<script src="https://hongyul67-cpu.github.io/links/reset.js"></script>
```

왼쪽 아래에 `🧹 기록 초기화` 버튼이 생깁니다. 도구마다 저장 키 이름이 달라서
키를 적어 둘 필요는 없고, 열려 있는 저장소를 훑어 스스로 분류합니다.

| 묶음 | 기본 | 내용 |
|---|---|---|
| 학습 기록 | 지움 | 진도 · 이어하기 · 오답노트 |
| 계급 · 랭크 | 지움 | `rank_v1_*` |
| 내 정보 | 지움 | 이름 · 반 · 번호 (`rc_student`) |
| 잠금 해제 | **안 지움** | 지우면 코드를 다시 넣어야 함 |
| 교사 결과수집 주소 | **안 지움** | 지우면 제출이 선생님께 안 감 |

> 교사 설정을 기본에서 빼 둔 이유 — 학생이 무심코 눌렀을 때 수업이 멈추면 안 되기 때문입니다.
> 둘 다 체크로 켤 수 있고, 켜면 무슨 일이 생기는지 화면에 적혀 있습니다.

수업모드에서는 자동으로 숨습니다(`.cm-chrome`).

---

## 4. git — 이걸 빠뜨리면 충돌납니다

여러 세션이 같은 도구를 동시에 고치는 일이 실제로 있었습니다.

```bash
git fetch origin
git rebase origin/main          # links 저장소는 origin/master
# 합쳐진 파일에서 내 변경과 남의 변경이 둘 다 살아있는지 확인
git push origin main
```

- **다른 사람 작업을 지우지 마세요.** 리베이스로 그 위에 얹습니다.
- `git reset --hard`, `git push --force`는 쓰지 마세요.
- push 후 GitHub Pages 빌드가 끝나야 실제 페이지에 반영됩니다.
  ```bash
  gh api repos/hongyul67-cpu/<저장소>/pages/builds/latest --jq .status
  ```

---

## 5. 검증

**문법 검사** — `<script>` 안쪽만 뽑아서 확인합니다.

```bash
python -c "
import io,re
s=io.open('index.html',encoding='utf-8').read()
js='\n'.join(m.group(1) for m in re.finditer(r'<script(?![^>]*\bsrc=)[^>]*>(.*?)</script>', s, re.S))
io.open('_chk.js','w',encoding='utf-8').write(js)"
node --check _chk.js
```

**도형 검사** — 좌표를 눈으로 믿지 말고 `getBBox()`로 겹침을 계산하세요.
치수선과 가상선이 겹쳐 그림이 깨져 보이는 사고가 있었습니다.

**클릭 검증** — 미리보기 창의 마우스 클릭이 자주 빗나갑니다(페이지 스크롤과
좌표계가 어긋남). 화면 이동은 앱 함수를 불러 진행하더라도, **판정은 실제 렌더링
좌표와 실제 전송 본문으로** 하세요. 상태를 직접 세팅해 통과시키지 마세요.

---

## 6. 만들 때의 원칙

- **배우기(정답을 다 보여주는 단계)를 앞에, 테스트를 뒤에.** 학생도 교사도 비전문가입니다.
- 정확한 개념 → 게임 → 재미, 세 가지를 모두 갖춥니다.
- 용어는 **교과서 표현**을 따르고, 필요하면 짧은 보충 설명만 덧붙입니다.
- 반응·콤보·진행 시각화·클리어 배너·보상까지 만듭니다.
- 문제 캡션이 정답을 흘리지 않는지 확인합니다.
- 거대한 앱 하나보다 **영역·과목별로 파일과 저장소를 나눕니다.**
- 새 도구는 링크 허브(`links-master/master.html`)에 카드로 붙입니다.
